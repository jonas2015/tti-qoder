# Render.com Blueprint
# Deploy complete TTi Group application (Frontend + Backend) using single Docker

services:
  # Single service for both frontend and backend
  - type: web
    name: tti-group
    env: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
